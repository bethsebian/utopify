<tr>

  <td id="order_status"><%= order.status %></td>

  <td id="order_total_price"><%= number_to_currency(order.total_price) %></td>

  <td id="order_created_at"><%= order.created_at %></td>

  <td><%= link_to "View Order ##{"%07d" % order.id.to_s}", order_path(order.id) %></td>

</tr>