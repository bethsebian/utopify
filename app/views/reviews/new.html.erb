<%= form_for(@review, url: item_reviews_path(params[:item_id])) do |f| %>
  <%= f.label :text %>
  <%= f.text_field :text %>

  <%= f.label :stars %>
  <%= f.text_field :stars %>

  <%= f.submit "Create Review" %>
<% end %>
