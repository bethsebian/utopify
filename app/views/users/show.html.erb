<h1> <%= @user.first_name %>'s Profile (<%= @user.username %>)</h1>
<h5 class="center">DoomsDay Meter:</h5>

<%= render partial: 'partials/doomsday_meter' %>

<h2><%= link_to "View Past Orders", orders_path %></h2>

<h3>All Reviews for <%= @user.username %>:</h3>
<ul>
  <div class="container">
    <%= render partial: 'partials/review_card_dashboard', collection: @reviews, as: :review %>
  </div>
</ul>
